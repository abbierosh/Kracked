# Kracked

KRackedbakes flavour finder quiz.

This is a static HTML/CSS/JavaScript site that recommends a KRackedbakes product based on quiz answers.

## Run Locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000/`.
